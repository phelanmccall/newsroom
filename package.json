{
  "name": "newsroom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && nodemon server.js\" \"cd client && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["react", "newsroom", "news"],
  "author": "Phelan McCall",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
